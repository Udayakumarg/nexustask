{
  "name": "project-nexus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "node ../framework/scripts/init-demo-project.js",
    "clean": "npx rimraf dist tsconfig.tsbuildinfo",
    "build": "tsc --build --force",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20.19.37",
    "dotenv": "^17.3.1",
    "node-fetch": "^2.6.7",
    "rimraf": "^5.0.0",
    "simple-playwright-framework": "^0.0.20"
  }
}
